#This block displays the date
#!/bin/bash

date
#What I found interesting is how very simplified this code is in comparison to java, where you'd likely have to import some package to gain access to simplified direct date commands
